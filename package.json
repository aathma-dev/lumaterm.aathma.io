{
  "name": "lumaterm.aathma.io",
  "version": "1.0.0",
  "devDependencies": {
    "@types/bun": "latest",
    "gh-pages": "^6.3.0",
    "vite": "6"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "bun run build && gh-pages -d dist -b gh-pages"
  },
  "type": "module"
}
